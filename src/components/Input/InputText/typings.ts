export type MapStateToPropsType = {};

export type MapDispatchToPropsType = {};

export type OwnPropsType = {
  // Выводит текст placeholdera
  plsText: string;
  // Имя input (используется при валидайии)
  inputName: string;
};
