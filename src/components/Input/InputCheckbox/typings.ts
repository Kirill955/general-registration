export type MapStateToPropsType = {};

export type MapDispatchToPropsType = {};

export type OwnPropsType = {
  // Имя inputa
  name: string;
  // Id inputa
  id: string;
  // Текст label
  labelText: string;
  // htmlFor
  htmlFor: string;
};
